<?php require('template/header.phtml'); ?>

<div class="container mt-4">
    <div class="row">

        <!-- Profile Sidebar -->
        <div class="col-md-4">
            <div class="card shadow-sm mb-4 profile-card">
                <div class="card-body text-center">

                    <img src="/images/profilepicture.png"
                         class="rounded-circle mb-3"
                         style="width:150px; height:150px; object-fit:cover;">

                    <h4><?= htmlspecialchars($view->profile['name']); ?></h4>
                    <p class="text-muted"><?= htmlspecialchars($view->profile['email']); ?></p>

                    <p><strong>Member Since:</strong> <?= $view->profile['joined']; ?></p>
                </div>
            </div>

            <!-- Friends List -->
            <div class="card shadow-sm profile-card">
                <div class="card-header bg-danger text-white">
                    <h6 class="mb-0">Friends</h6>
                </div>
                <div class="card-body">

                    <?php foreach ($view->friends as $friend): ?>
                        <div class="d-flex justify-content-between align-items-center border-bottom py-2">
                            <strong><?= htmlspecialchars($friend['name']); ?></strong>
                            <span class="badge bg-info text-dark">
                                <?= $friend['modulesCompleted']; ?>%
                            </span>
                        </div>
                    <?php endforeach; ?>

                </div>
            </div>
        </div>

        <!-- Dashboard / Modules Progress -->
        <div class="col-md-8 mt-4 mt-md-0">

            <div class="card shadow-sm mb-4 profile-card">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Your Progress</h5>
                </div>
                <div class="card-body">

                    <?php foreach ($view->profile['modules'] as $module): ?>
                        <div class="mb-3">
                            <h6 class="mb-1"><?= htmlspecialchars($module['title']); ?></h6>

                            <div class="progress" style="height:20px;">
                                <div class="progress-bar bg-info"
                                     style="width: <?= $module['completed']; ?>%">
                                    <?= $module['completed']; ?>%
                                </div>
                            </div>
                        </div>
                    <?php endforeach; ?>

                </div>
            </div>

        </div>
    </div>
</div>

<?php require('template/footer.phtml'); ?>

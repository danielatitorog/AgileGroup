<?php require('template/header.phtml'); ?>
<div class="register-page mt-3 d-flex flex-column justify-content-center align-items-center">
    <!-- Login card container -->
    <div class="register-card px-4 py-2 shadow bg-white" style="max-width: 400px; width: 100%; border-radius: 20px;">
        <h2>Create an Account</h2>

        <?php if (!empty($view->errorMessage)): ?>
            <div class="alert alert-info text-center" role="alert">
                <?= $view->errorMessage ?>
            </div>
        <?php endif; ?>

        <form method="POST" action="register.php">
            <div class="row g-3 mb-3">
                <div class="col-12">
                    <label for="username" class="form-label fw-semibold">Username:</label>
                    <input type="text" name="username" id="username" class="form-control" required>
                </div>
                <div class="col-12">
                    <label for="email" class="form-label fw-semibold">Email:</label>
                    <input type="email" name="email" id="email" class="form-control" required>
                </div>
                <div class="col-12">
                    <label for="password" class="form-label fw-semibold">Password:</label>
                    <input type="password" name="password" id="password" class="form-control" required>
                    <small class="text-muted">
                        Password must be at least 12 characters long and include uppercase, lowercase, numbers, and symbols.
                        It must not contain dictionary words or names.
                    </small>
                </div>
                <div class="col-12">
                    <label for="confirm" class="form-label fw-semibold">Confirm Password:</label>
                    <input type="password" name="confirm" id="confirm" class="form-control" required>
                </div>
            </div>
            <button type="submit" name="registerBtn" class="btn btn-danger w-100 fw-semibold">Sign Up</button>
        </form>

        <p class="mt-3">Already have an account? <a href="index.php" class="login-link">Log in</a></p>
    </div>
</div>

<?php require('template/footer.phtml'); ?>

